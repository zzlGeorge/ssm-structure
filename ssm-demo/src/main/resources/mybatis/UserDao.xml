<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.vct.ssm.dao.UserDao">

    <sql id="columns">`id`,`user_name`,`email`,`create_time`,`modify_time`</sql>

    <resultMap id="UserMap" type="com.vct.ssm.domain.UserDO">
        <id property="id" column="id"/>
        <result property="userName" column="user_name"/>
        <result property="email" column="email"/>
        <result property="createTime" column="create_time"/>
        <result property="modifyTime" column="modify_time"/>
    </resultMap>

    <select id="get" resultType="com.vct.ssm.domain.UserDO">
        select
        <include refid="columns"/>
        from van_user where id = #{value}
    </select>

    <select id="list" resultType="com.vct.ssm.domain.UserDO">
        select
        <include refid="columns"/>
        from van_user
        <where>
            <if test="id != null and id != ''">and id = #{id}</if>
            <if test="userName != null and userName != ''">and user_name = #{userName}</if>
            <if test="email != null and email != ''">and email = #{email}</if>
            <if test="createTime != null and createTime != ''">and create_time = #{createTime}</if>
            <if test="modifyTime != null and modifyTime != ''">and modify_time = #{modifyTime}</if>
        </where>
        <choose>
            <when test="sort != null and sort.trim() != ''">
                order by ${sort} ${order}
            </when>
            <otherwise>
                order by id desc
            </otherwise>
        </choose>
        <if test="offset != null and limit != null">
            limit #{offset}, #{limit}
        </if>
    </select>

    <select id="count" resultType="java.lang.Integer">
        select count(*) from van_user
        <where>
            <if test="id != null and id != ''">and id = #{id}</if>
            <if test="userName != null and userName != ''">and user_name = #{userName}</if>
            <if test="email != null and email != ''">and email = #{email}</if>
            <if test="createTime != null and createTime != ''">and create_time = #{createTime}</if>
            <if test="modifyTime != null and modifyTime != ''">and modify_time = #{modifyTime}</if>
        </where>
    </select>

    <insert id="save" parameterType="com.vct.ssm.domain.UserDO" useGeneratedKeys="true" keyProperty="id">
		insert into van_user
		(
                		 `user_name`,
		             		 `email`,
		             		 `create_time`,
		             		 `modify_time`        		)
		values
		(
                		#{userName},
		            		#{email},
		            		#{createTime},
		            		#{modifyTime}        		)
	</insert>

    <update id="update" parameterType="com.vct.ssm.domain.UserDO">
        update van_user
        <set>
            <if test="userName != null">`user_name` = #{userName},</if>
            <if test="email != null">`email` = #{email},</if>
            <if test="createTime != null">`create_time` = #{createTime},</if>
            <if test="modifyTime != null">`modify_time` = #{modifyTime}</if>
        </set>
        where id = #{id}
    </update>

    <delete id="remove">
		delete from van_user where id = #{value}
	</delete>

    <delete id="batchRemove">
        delete from van_user where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>